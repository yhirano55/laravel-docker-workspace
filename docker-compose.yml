version: '3'

services:
  app:
    image: php:7.4-fpm
    volumes:
      - .:/var/www/html
  web:
    image: nginx:1.17.8
    ports:
      - '8000:80'
    depends_on:
      - app
    volumes:
      - ./docker/web/default.conf:/etc/nginx/conf.d/default.conf
      - .:/var/www/html
